# Netdog / Executables
These are executables for Netdog.
`netdog-linux` is the **Linux executable**.
`netdog-windows.exe` is the **Windows executable**.
Sadly, I cannot make a MacOS executable, but the python version runs fine. And, if you do want to make one, feel free to. I used PyInstaller for both of these.
